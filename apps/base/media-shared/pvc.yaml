apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-library
  namespace: media
spec:
  accessModes: ["ReadWriteMany"]   # shared across nodes — pods can land on either worker
  storageClassName: nfs-client
  resources:
    requests:
      storage: 500Gi
